#include <stdio.h>
int main()
{
    char a,b,c,d;
    printf("Enter the characters: ");
    a=getchar();
    b=getchar();
    c=getchar();
    d=getchar();

    printf("The first char is '%c', its value is %d \n",a,a);
    printf("The first char is '%c', its value is %d \n",b,b);
    printf("The first char is '%c', its value is %d \n",c,c);
    printf("The first char is '%c', its value is %d \n",d,d);
}
